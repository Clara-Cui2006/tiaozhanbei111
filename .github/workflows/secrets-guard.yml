name: Secrets Guard

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  scan-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run plaintext secrets scan
        run: bash scripts/check_no_plain_secrets.sh
