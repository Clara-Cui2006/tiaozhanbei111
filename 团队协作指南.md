# 团队协作指南

## 项目信息

- **仓库地址**：https://github.com/aleafofwutong/Tiaozhanbei.git
- **技术栈**：Vue3 + Vite + TypeScript（前端）/ Python（后端）

---

## 第一次克隆项目

### 1. 配置 Git 代理（国内网络需要，端口改成自己的）
```bash
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897
```

### 2. 克隆仓库
```bash
git clone https://github.com/aleafofwutong/Tiaozhanbei.git
cd Tiaozhanbei
```

### 3. 安装前端依赖
```bash
npm install
```

### 4. 安装 Python 依赖
```bash
# 需要 Python 3.10+，推荐用 Homebrew 安装 Python 3.12
brew install python@3.12

# 创建并激活虚拟环境
/opt/homebrew/bin/python3.12 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements-server.txt
```

---

## 日常开发流程

### 每天开始工作前：拉取最新代码
```bash
git pull origin main
```

### 开发中：频繁提交
```bash
git add .
git commit -m "feat: 描述你做了什么"
```

### 完成后：推送到远程
```bash
git push origin main
```

---

## 同步队友代码（详细教程）

### 场景一：查看队友提交了什么

```bash
# 第一步：进入项目目录
cd /Users/你的用户名/Desktop/claudecode/Tiaozhanbei

# 第二步：拉取远程最新状态（不会覆盖你本地的文件）
git fetch origin

# 第三步：查看队友最近的提交记录
git log origin/main --oneline
```

输出示例：
```
01b7caf docs: 添加团队协作指南   ← 最新的
a3c12ef feat: 完成登录页面
9d4f001 feat: 初始化项目
```
每一行就是一次提交，可以看到谁做了什么。

---

### 场景二：把队友的代码同步到自己本地

```bash
# 一条命令搞定，拉取并合并到本地
git pull origin main
```

- 如果显示 `Already up to date.` → 本地已经是最新的，无需操作
- 如果显示一堆文件名 → 同步成功，队友的改动已合并到你本地

---

### 场景三：查看具体改动了哪些内容

```bash
# 查看某次提交改了什么（把 commit编号 换成 git log 里看到的那串字母数字）
git show 01b7caf

# 查看队友和你之间的差异（你本地 vs 远程最新）
git diff HEAD origin/main
```

---

### 场景四：不小心和队友改了同一个文件（冲突）

执行 `git pull` 时如果出现冲突提示：

```bash
# 第一步：查看哪些文件冲突了
git status
# 冲突文件会显示 "both modified"

# 第二步：打开冲突文件，会看到类似内容：
# <<<<<<< HEAD
# 你的代码
# =======
# 队友的代码
# >>>>>>> origin/main

# 第三步：手动编辑文件，保留正确内容，删掉标记符号

# 第四步：标记冲突已解决
git add 冲突的文件名
git commit -m "fix: 解决与队友的合并冲突"
```

**💡 预防冲突的最佳方式：每次开始写代码前先 `git pull`**

---

### 日常同步节奏建议

```
早上开始写代码
  └── git pull origin main        ← 先同步队友昨天的进度

写完一个小功能
  └── git add .
  └── git commit -m "feat: xxx"   ← 随时提交，不要攒

下班/休息前
  └── git push origin main        ← 推送，让队友明天能看到你的进度
```

---

## 运行项目

### 启动前端
```bash
npm run dev
# 访问 http://localhost:5173/
```

### 激活 Python 环境（每次使用后端前）
```bash
source venv/bin/activate
# 退出虚拟环境
deactivate
```

---

## 提交信息规范

| 前缀 | 含义 |
|------|------|
| `feat:` | 新功能 |
| `fix:` | 修复 bug |
| `docs:` | 文档更新 |
| `style:` | 格式调整 |
| `refactor:` | 代码重构 |

示例：`git commit -m "feat: 完成用户登录页面"`

---

## 解决冲突

```bash
# 拉取时出现冲突，手动编辑冲突文件后：
git add 冲突文件
git commit -m "fix: 解决合并冲突"
git push
```

**预防冲突：每天开始工作前先 `git pull`**

---

## 注意事项

- `venv/` 文件夹不会上传到 GitHub（已在 .gitignore 中）
- 不要提交 `.env` 等包含密钥的文件
- 推送前先 pull，避免冲突
